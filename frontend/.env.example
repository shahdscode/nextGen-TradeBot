# Local dev (Vite proxy handles /api when empty — or point directly):
# VITE_API_URL=
#
# Vercel production — set in Vercel Dashboard → Settings → Environment Variables:
# VITE_API_URL=https://YOUR-BACKEND-URL.com
#
# Example local direct backend:
VITE_API_URL=http://127.0.0.1:8002
